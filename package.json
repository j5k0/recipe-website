{
  "name": "recipe-website",
  "private": true,
  "scripts": {
    "test": "npm test -w @my-app/backend",
    "test:integration": "npm run test:integration -w @my-app/backend"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@supabase/supabase-js": "^2.98.0",
    "bcrypt": "^6.0.0",
    "multer": "^2.1.0"
  }
}
